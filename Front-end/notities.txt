Tijdentabel:

addEventlistener voor arrows => functie wordt uitgevoerd die nieuwe id's aan de cellen geeft.

in de database:
- werktijden vd kapper;
- geplande afspraken vd kapper;

schetch-code: 

Variables:
let bookedHours = [];

hourCells = document.getElementByClass(hourCells);

function creatingTable () {
	for (let j = 1; j <=63; j++ {
		hour += '<div class="hourcells" id=firstRow'+j+'>9:00-10:00</div>'
		if document.getElementsByClassName(
	


for (each cel in table) { 
	if (id in db) { 
		{color = grey & unclickable};
	}
};


for (each cel in table) {
	if (cel is clicked) {
		setlocalstorage(booked, [....,....]);




for loop 

cel id == c'


